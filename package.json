{
  "name": "blockhub",
  "private": true,
  "scripts": {
    "web-client": "cd clients && cd web && yarn dev",
    "web-service": "cd services && cd web && yarn dev",
    "desktop-client": "cd clients && cd desktop/app && yarn dev",
    "styleguide": "cd tools && cd styleguide && yarn dev"
  },
  "devDependencies": {
    "@nuxtjs/eslint-config": "^1.0.1",
    "babel-eslint": "^10.0.2",
    "lerna": "^3.8.0"
  },
  "dependencies": {
    "eslint": "^6.0.1"
  }
}